import React, {useEffect} from 'react'

import './teamInfo.scss'

export default function TeamInfo() {
  useEffect(() => {
    console.log('RENDER')
  })

  return (
    <main className='team-page'>TEAM?</main>
  )
}
